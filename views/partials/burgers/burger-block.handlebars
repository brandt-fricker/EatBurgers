<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}Eat MEOW!{{else}}Eat Me Again?{{/if}}
	</button>
	<button class="delete" data-id="{{id}}">
		DELETE
	</button>
</li>
